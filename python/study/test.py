def add(x, y):
    return x + y


def add(x, y): return x + y


# 创建列表框
list = tk.Listbox(win, font=('楷体', 16), width=40, height=20).place(x=10, y=36)
list.insert(END, '123')
def add(master_menu):
    def click():
        print(child_radio.get(), child_lower.get())
        print(checkbox.get(), checkbox1.get())
